**Shrinking Window**
```python
def two_pointers(arr):
	arr.sort()
	left = 0
	right = len(arr) - 1
	while left < right:
		left_val = arr[left]
		right_val = arr[right]
		while compare_left_right: # some condition is met
			# perform some action
			left += 1
	return #answer
```

**HASHMAP
```python
def two_pointers_hashmap(arr):
	
```
